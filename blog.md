# 一箭又一箭 —— Python + AIGC 小游戏开发

| 项目 | 内容 |
| --- | --- |
| 这个作业属于哪个课程 | <课程链接> |
| 这个作业要求在哪里 | <作业链接> |
| 这个作业的目标 | 使用 Python 和 AIGC 完成"一箭又一箭"小游戏 |
| 学号 | XXXXXXXX |
| GitHub 仓库 | https://github.com/otto2-5-8/one-arrow-after-another |

## 一、项目展示

| 开始界面 | 游戏界面 | 通关 | 失败 |
| --- | --- | --- | --- |
| ![菜单](screenshots/01_menu.png) | ![游戏](screenshots/02_level1.png) | ![通关](screenshots/06_win.png) | ![失败](screenshots/07_lose.png) |

箭头飞出棋盘、被挡住晃动、提示功能分别见仓库 `screenshots/03_flying.png`、`04_blocked.png`、`05_level5_hint.png`。

## 二、项目介绍

点击式箭头解谜。棋盘上每个箭头朝上、下、左、右之一，点击后沿箭头方向看：一路到棋盘边界都没有别的箭头就能飞出并消失；路上有箭头则飞不出去，箭头晃动变红并提示，失误次数减 1。清空全部箭头过关，失误用完本关失败。共 5 关（5×5 到 7×7），界面含开始界面、游戏界面、通关/失败界面，游戏界面显示关卡、棋盘、剩余箭头、剩余失误、用时，并有重新开始、提示、撤销、返回菜单按钮。附加功能：关卡选择与解锁、计时、提示（求解器）、撤销。

## 三、实现思路

箭头用 `{(行, 列): 方向}` 的字典存，方向就是 `^ v < >` 四个字符，关卡直接写成字符串数组：

```python
LEVELS = [{"name": "第 1 关", "mistakes": 3,
           "grid": [".....", "..>v.", "..v..", ".....", "..v<."]}]
```

路径检测是关键：从箭头出发沿方向一格一格走，遇到箭头就说明被挡住，走出棋盘说明可以飞出，所以左右上下共用同一段代码，不用分别处理越界：

```python
def is_free(self, pos):
    dr, dc = DIRS[self.arrows[pos]]
    r, c = pos[0] + dr, pos[1] + dc
    while 0 <= r < self.rows and 0 <= c < self.cols:
        if (r, c) in self.arrows:
            return False
        r += dr
        c += dc
    return True
```

界面用 pygame：`menu / game` 两个场景加通关失败面板；飞出动画是把箭头加入"飞行列表"每帧按方向移动，碰撞反馈是让该格左右晃动并变红。另外写了求解器 `solver.py`（记忆化搜索），既用来校验关卡有解，也实现提示功能。

## 四、AIGC 使用过程

| 子任务 | 借助何种 AIGC | AI 实现或提供了什么 | 效果如何 | 人工修改 |
| --- | --- | --- | --- | --- |
| 路径检测与求解器 | AIGC 编程助手（DeepSeek） | 四个方向的检测代码、记忆化搜索求解器、关卡校验脚本 | 求解器把"整局布局"当成"当前局面"判断阻挡，导致校验误报无解 | 改成 `free_in(state, dirs, ...)`：用当前剩余箭头判断阻挡、用原始布局查方向，恢复正常 |
| 关卡设计 | AIGC 编程助手（DeepSeek） | 随机生成"逆序摆放"的可解关卡候选 | 候选偏乱，人工挑选调整后第 5 关出现两处互相挡死的死局，无法通关 | 把两处箭头方向改掉，`tools/check_levels.py` 重新验证 5 关全可解 |
| 界面与动画 | AIGC 编程助手（DeepSeek） | pygame 界面框架、箭头绘制、飞出与晃动动画 | 能跑但运行即崩：`AttributeError: hint_timer`（新加的属性没在 `__init__` 初始化）；首帧 `dt` 含字体预加载耗时，动画和计时会跳一下 | 补上初始化；把 `dt` 限制在 0.1 秒内，并缓存字体路径加快启动 |
| 测试与截图 | AIGC 编程助手（DeepSeek） | T01-T06 测试用例、按剧本驱动界面的自动截图脚本 | 有一条测试用例的期望值写错（漏看了另一个箭头挡住了路径） | 核对路径后修正用例；截图脚本抓图偏移、窗口被遮挡、提示过期，逐项修好（DPI 感知、置顶、持续触发提示） |

## 五、测试结果

`python -m unittest discover -s tests -v`，9 项全部通过（含额外用例：四个方向阻挡、五个关卡均可通关）。

| 编号 | 测试内容 | 预期结果 | 实际结果 | 是否通过 |
| --- | --- | --- | --- | --- |
| T01 | 点击前方无阻挡的箭头 | 箭头飞出消失 | 剩余箭头 2→1，失误不变 | 通过 |
| T02 | 点击前方有阻挡的箭头 | 不消失，失误 -1 | 返回 blocked，失误 3→2 | 通过 |
| T03 | 点击边缘朝外的箭头 | 正常消失、不越界 | 四个方向均正常飞出 | 通过 |
| T04 | 消除本关全部箭头 | 显示通关并进入下一关 | 面板出现，解锁 2 关，下一关为第 2 关 | 通过 |
| T05 | 失误次数耗尽 | 显示失败并可重新开始 | result=lose，面板显示失败 | 通过 |
| T06 | 游戏中重新开始 | 布局与失误恢复 | 箭头 5，失误 3，用时清零 | 通过 |

## 六、PSP

| 任务 | 预估（小时） | 实际（小时） | 差异（小时） |
| --- | --- | --- | --- |
| 需求分析与游戏设计 | 2 | 1.5 | -0.5 |
| Python 与图形库学习 | 3 | 2 | -1 |
| 游戏界面实现 | 4 | 5 | +1 |
| 路径与碰撞逻辑实现 | 3 | 2.5 | -0.5 |
| 关卡设计 | 2 | 1.5 | -0.5 |
| AIGC 辅助开发 | 3 | 4 | +1 |
| 测试与修改 | 2 | 2.5 | +0.5 |
| README 与博客撰写 | 2 | 1.5 | -0.5 |
| 合计 | 21 | 20.5 | -0.5 |

## 七、心得体会

AI 能很快给出可运行的框架和动画代码，省下了查 API 的时间，但它写的代码不一定对：求解器的阻挡判断、关卡的死局、漏掉的属性初始化，都是我自己跑起来才发现的。所以关键逻辑（路径检测、通关校验）必须自己理一遍并用测试固定下来，AI 生成的东西也要自己试玩确认，这也是这次作业最大的收获。
